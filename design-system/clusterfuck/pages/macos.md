# macOS overrides — ClusterFuck control window

MASTER clinical primary `#0284C7` unchanged. Window chrome follows dark HUD (`#0F0F23`) because this is a Crooks control surface, not a marketing page.

**Design dials:** Variance 3 · Motion 3 · Density 7

## Layout

- `NavigationSplitView` with the four remote pages.
- Minimum window 420×560 (existing). Keyboard: default button = Minimize σ when session is live.
- App sandbox + outgoing network (Spotify / Alexa / Sonos / DI.fm). No iOS `LSRequiresIPhoneOS` keys.

## Icon

Mac App Store icon is the concentric Crooks gauge (1024 RGB opaque PNG). Same family as iOS/watch, more tick marks.
